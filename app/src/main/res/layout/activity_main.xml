<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_light">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- App Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="QuizzIt"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="#2E2D32"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="8dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Smart Quiz Generation App"
            android:textSize="16sp"
            android:textColor="#3F3D56"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="32dp" />

        <!-- Create Quiz Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnCreateQuiz"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Create Quiz"
            android:textColor="@android:color/white"
            android:textAllCaps="false"
            app:backgroundTint="#6C63FF"
            app:cornerRadius="14dp"
            android:layout_marginBottom="16dp" />

        <!-- Test Gemini Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnTestGemini"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Test AI Quiz Generation"
            android:textColor="@android:color/white"
            android:textAllCaps="false"
            app:backgroundTint="#FF5722"
            app:cornerRadius="14dp"
            android:layout_marginBottom="24dp" />

        <!-- Optional info / instructions -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tap 'Create Quiz' to make a new quiz.\nTap 'Test AI Quiz Generation' to see an example question."
            android:textColor="#3F3D56"
            android:textSize="14sp"
            android:layout_gravity="center_horizontal"
            android:gravity="center" />

    </LinearLayout>
</ScrollView>
